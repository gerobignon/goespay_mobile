/**
 * Route « Détail d'une opération crypto ». L'écran vit dans `src/screens` pour
 * que Metro puisse lui substituer une variante iOS : le binaire iOS n'embarque
 * aucune fonctionnalité crypto (règle App Store 3.1.5(iii)).
 */
export { default } from '../../../src/screens/CryptoTransactionScreen';
